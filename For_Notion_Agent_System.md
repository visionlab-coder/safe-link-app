# 🤖 AI Agent 3-Layer Architecture & Protocol

> 이 문서는 AI 에이전트와 협업할 때 신뢰성과 확장성을 보장하기 위한 **3계층 아키텍처**와 **프로젝트 적용 표준**을 정리한 것입니다.

---

## 1. 핵심 철학: 3-Layer Architecture

AI(LLM)는 확률적이지만, 비즈니스 로직은 결정론적이어야 합니다. 이 불일치를 해결하기 위해 역할을 3단계로 분리합니다.

### 🏗 Layer 1: Directive (지시 - What)
*   **역할**: 무엇을 해야 하는지 정의 (SOP)
*   **위치**: `directives/*.md`
*   **내용**: 목표, 입력값, 사용할 도구, 예외 처리 규칙이 담긴 자연어 매뉴얼.

### 🧠 Layer 2: Orchestration (조율 - Decision)
*   **역할**: AI 에이전트 (판단과 결정)
*   **위치**: AI(나)의 사고 과정
*   **내용**: 지시서를 읽고, 적절한 도구(Layer 3)를 선택하여 실행하고, 결과를 판단하여 다시 지시서를 업데이트하는 "두뇌" 역할.

### ⚙️ Layer 3: Execution (실행 - Doing)
*   **역할**: 결정론적 작업 수행
*   **위치**: `execution/*.py` (또는 스크립트)
*   **내용**: API 호출, 데이터 파싱 등 실수를 용납하지 않는 작업은 반드시 코드로 작성하여 실행.

---

## 2. 주요 운영 원칙 (Operating Principles)

1.  **도구 우선 (Check for tools first)**: 무작정 코드를 짜지 말고, `execution/`에 이미 만들어진 도구가 있는지 확인한다.
2.  **자가 치유 (Self-annealing)**: 에러가 발생하면 고치는 데서 끝내지 말고, **"왜 실패했는지"**를 배워서 지시서(Directive)에 업데이트한다. (시스템이 스스로 똑똑해짐)
3.  **지시서의 생명력**: 지시서는 단순한 텍스트가 아니라, 에이전트의 **행동 강령(Instruction Set)**이므로 지속적으로 업데이트하고 관리한다.

---

## 3. 실전 적용: Safe-Link 프로젝트 사례

이 아키텍처는 복잡한 시스템(Safe-Link)에서 다음과 같은 효과를 발휘합니다:

*   **기능 격리**: 번역, TTS 등 복잡한 로직을 `execution` 레이어로 분리하여 웹 앱의 안정성 확보.
*   **에러 방지**: API 한계 등을 학습하여 매뉴얼화함으로써 동일한 에러 반복 방지.
*   **명확한 판단**: 상황에 따라 적절한 모델(Gemini vs Google TTS)을 선택하는 의사결정 체계 확립.

---

## 4. 모든 프로젝트에 적용하기 (표준 프로토콜)

새로운 프로젝트를 시작할 때, 아래 3단계를 수행하여 AI 친화적인 환경을 구축합니다.

### ✅ Step 1: 헌법 제정 (`AGENTS.md`)
프로젝트 루트에 `AGENTS.md` 파일을 생성하고 3계층 아키텍처 내용을 명시합니다. 이것이 AI가 가장 먼저 읽는 "헌법"이 됩니다.

### ✅ Step 2: 작업 공간 세팅 (Scaffolding)
다음 3가지 폴더를 필수적으로 생성합니다:
*   📂 `directives/` : 작업 지시서 저장
*   📂 `execution/` : 실행 스크립트 저장
*   📂 `.tmp/` : 임시 파일 저장 (Git 제외)

### ✅ Step 3: 초기화 자동화
AI에게 다음과 같이 요청합니다:
> "새 프로젝트야. `AGENTS.md`를 만들고 기본 폴더 구조(`directives`, `execution`, `.tmp`)를 세팅해 줘."

---

## 📁 파일 구조 예시

```text
my-project/
├── AGENTS.md          # AI 행동 강령
├── directives/        # 작업 지시서 (Markdown)
│   ├── deploy_guide.md
│   └── translation_rules.md
├── execution/         # 실행 도구 (Python/Scripts)
│   ├── scrape_data.py
│   └── batch_translate.py
├── .tmp/              # 임시 파일 (GitIgnored)
└── .gitignore         # .tmp/ 포함
```
